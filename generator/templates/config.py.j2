"""Configuration for the inference server — generated by UniML."""

import os


class Settings:
    MODEL_PATH: str = os.getenv("MODEL_PATH", "model.onnx")
    HOST: str = os.getenv("HOST", "{{ host }}")
    PORT: int = int(os.getenv("PORT", "{{ port }}"))


settings = Settings()
