# ============================================================
# UniML — Universal ML Deployment Platform
# Requirements
# ============================================================

# --- Core Framework ---
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-multipart>=0.0.9

# --- HTTP client (tests / tooling) ---
requests>=2.31.0,<3.0.0

# --- ML Core (always installed) ---
numpy>=1.26.0

scikit-learn>=1.4.0
joblib>=1.3.0
onnx>=1.16.0
onnxruntime>=1.18.0
skl2onnx>=1.17.0

# --- ML Optional (PyTorch / TensorFlow conversion) ---
# Declared in requirements-convert.txt and baked into the deployment image so
# conversion runs reliably server-side. For full support without Docker, run:
#   pip install -r requirements-convert.txt

# --- Code Generation ---
Jinja2>=3.1.0,<4.0.0

# --- Utilities ---
psutil>=5.9.0,<6.0.0


# --- Testing ---
pytest>=8.1.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
httpx>=0.27.0,<1.0.0

# --- Logging ---
loguru>=0.7.0,<1.0.0
